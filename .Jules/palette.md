## 2026-06-22 - Component specific UI/A11y learnings\n**Learning:** In the Crystal playground, layout ecr templates must define `@styles` and `@scripts` array types when evaluating them for a syntax check, otherwise evaluating them fails.\n**Action:** Remember to pass instance variables like `@styles = [] of String; @scripts = [] of String;` to `crystal eval` when syntax checking ECR templates.
